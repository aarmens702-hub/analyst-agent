{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) UV=\"$HOME/.local/bin/uv\"; [ -x \"$UV\" ] || UV=uv; cd \"$CLAUDE_PROJECT_DIR\" && \"$UV\" run ruff check --fix \"$f\" && \"$UV\" run ruff format \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ruff on changed file"
          }
        ]
      }
    ]
  }
}
