# python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/

# project
data/
workspace/
.env
.env.*

# claude / tdd-guard runtime state
.claude/tdd-guard/
.claude/settings.local.json

# the skill library: skill folders are authored artifacts and belong in git —
# a PR showing a new skill is the human admission gate leaving a paper trail.
# The ledger is not: it rewrites on every application, so committing it turns
# each run into a diff of incrementing counters.
skills/ledger.json
skills/retired/

# the Loop application is personal, not repo content
takehome/

# macOS noise
.DS_Store
bench/results/
